name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - sphinx
    - asteroid-sphinx-theme
    - codecov
    - cmselemental
    - mmelemental
    - git+https://github.com/MolSSI/mmic
